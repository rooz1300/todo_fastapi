[project]
name = "todo-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "roozbeh badiei", email = "rooz.un1300@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.19.1",
    "fastapi[all]>=0.141.1",
    "passlib[bcrypt]",
]

[project.scripts]
todo-fastapi = "todo_fastapi:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
